# Continuity notes

A working document, not part of the novel. The contents file links to it
from a paragraph rather than a list item, which is what keeps it out of the
book — see the linked-list rules in `../book_template_md/README.md`.
